# api/v1

Protobuf resource definitions (`Pod`, `Node`, `Deployment` simplifié) live
here once defined — see design doc section 11 (`google.golang.org/protobuf`)
and section 12, step B ("Détailler le schéma Protobuf").

Reserved, empty until step B.
