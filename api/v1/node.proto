syntax = "proto3";

package nimbuscore.v1;

import "common.proto";

option go_package = "github.com/gh0st-nemesis/nimbuscore/api/v1;v1";

message NodeSpec {
  bool unschedulable = 1;
}

message NodeStatus {
  ResourceList capacity = 1;
  ResourceList allocatable = 2;
  bool ready = 3;
  repeated Condition conditions = 4;
  int64 last_heartbeat_unix = 5;
  string internal_ip = 6;
}

message Node {
  ObjectMeta metadata = 1;
  NodeSpec spec = 2;
  NodeStatus status = 3;
}
